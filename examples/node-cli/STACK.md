# node-cli example

Small local-only library using the language standard library. Preserve finite-number validation and exact results within the declared numeric representation. The configured unit command is the complete test tier. No hosted API, database, browser, deployment, secrets or web performance budgets apply. Rollback is a source revision revert.
