# Browser tests

Use isolated fixtures and condition-based waits. Attach a shared console/error watcher before navigation and verify it after the test. Record justified third-party noise explicitly. Missing credentials and skipped integrations are coverage gaps, not passes. Test applicable viewports from STACK.md.
