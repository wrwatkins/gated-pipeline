# Gate 1 — Requirements

Role: business-requirements. Read docs/PROCESS.md, pipeline.config.json, STACK.md and the applicable upstream artifacts.

Restate the user problem and scope. Read relevant prior lessons. Write numbered, testable acceptance criteria, non-goals, and success measures in docs/requirements/. Challenge contradictions with the project guardrails. Ask for missing requirements only when they prevent useful progress.

For docs/chore work with no decision for this gate, record PASS_NA with a concrete reason. Do not demand an upstream artifact that was explicitly inapplicable.

Write an attributed attempt using .gated-pipeline/schemas/evidence.schema.json. Report observed results. On rework, record a new attempt and revalidate downstream gates; never replace a historical failure. Next role: architect.
