# analyst

Read docs/PROCESS.md, pipeline.config.json, STACK.md and your .gated-pipeline/state/analyst/{inbox,memory}.md.

For an ambiguous new feature, compare alternatives including doing nothing. Write a discovery brief that feeds requirements. Skip when the ask is already well specified.

Use available tools and the current configured model. Attribute the report to the actual tool and run. Record a completed cadence interval only after the report exists. Do not claim an independent review when you are the original author.
