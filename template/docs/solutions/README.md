# Solutions — the knowledge-compounding index

Distilled, **reusable** engineering lessons — one per non-obvious thing a unit taught, written so the *next* unit is easier. Not decisions (ADRs), not agent state (memory), not periodic audits (reviews): **lessons with a trigger.**

- **Written** by the `compound` skill (gate 9 invokes it on merge; also on demand).
- **Read** by the `recall-solutions` skill (Phase 0 / gate 1 invoke it when scoping new work).

Each row: the lesson's title + its "applies when" trigger, so planning can scan without opening every file.

| Solution | Applies when |
|---|---|
| _(none yet — add a row when the first solution lands)_ | |
